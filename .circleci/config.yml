version: 2.1
jobs:
  build:
    docker:
      - image: cimg/base:2022.12
    resource_class: small
    steps:
      - run: git --version
      - run: docker --version
      - run: echo "construyendo..."
  test:
    docker:
      - image: cimg/base:2022.12
    steps:
      - run: echo "testeando..."

workflows:
  build_and_test:
    jobs:
      - build
      - test:
          requires:
            - build
